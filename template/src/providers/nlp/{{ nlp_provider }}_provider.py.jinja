from providers.base import ProviderBase
from config.settings import settings

class {{ nlp_provider|capitalize }}Provider(ProviderBase):
    """
    Provider for {{ nlp_provider|capitalize }}
    """
    def __init__(self):
        self.api_key = settings.{{ nlp_provider }}_api_key
        if not self.api_key:
            raise ValueError("{{ nlp_provider|upper }}_API_KEY not set.")

    def example(self):
        return "Hello from {{ nlp_provider|capitalize }}Provider"

    def call(self, prompt):
        # TODO: Implement real API call for {{ nlp_provider|capitalize }}
        return f"Echo: {prompt}"
