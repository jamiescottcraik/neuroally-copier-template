from providers.base import ProviderBase
from config.settings import settings

class {{ speech_provider|capitalize }}Provider(ProviderBase):
    """
    Provider for {{ speech_provider|capitalize }}
    """
    def __init__(self):
        self.api_key = settings.{{ speech_provider }}_api_key
        if not self.api_key:
            raise ValueError("{{ speech_provider|upper }}_API_KEY not set.")

    def example(self):
        return "Hello from {{ speech_provider|capitalize }}Provider"

    def call(self, prompt):
        # TODO: Implement real API call for {{ speech_provider|capitalize }}
        return f"Echo: {prompt}"
