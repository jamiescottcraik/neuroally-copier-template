from providers.base import ProviderBase
from config.settings import settings

class {{ generative_provider|capitalize }}Provider(ProviderBase):
    """
    Provider for {{ generative_provider|capitalize }}
    """
    def __init__(self):
        self.api_key = settings.{{ generative_provider }}_api_key
        if not self.api_key:
            raise ValueError("{{ generative_provider|upper }}_API_KEY not set.")

    def example(self):
        return "Hello from {{ generative_provider|capitalize }}Provider"

    def call(self, prompt):
        # TODO: Implement real API call for {{ generative_provider|capitalize }}
        return f"Echo: {prompt}"
