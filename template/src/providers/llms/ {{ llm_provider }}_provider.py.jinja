from providers.base import ProviderBase
from config.settings import settings

class {{ llm_provider|capitalize }}Provider(ProviderBase):
    """
    Provider for {{ llm_provider|capitalize }}
    """
    def __init__(self):
        self.api_key = settings.{{ llm_provider }}_api_key
        if not self.api_key:
            raise ValueError("{{ llm_provider|upper }}_API_KEY not set.")

    def example(self):
        return "Hello from {{ llm_provider|capitalize }}Provider"

    def call(self, prompt):
        # TODO: Implement real API call for {{ llm_provider|capitalize }}
        return f"Echo: {prompt}"
